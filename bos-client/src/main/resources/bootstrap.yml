#配置zookeeper注册服务器地址
spring:
  cloud:
    zookeeper:
      discovery:
        root: spring-cloud-zookeeper
        register: false
      connectString: localhost:2181 # 多节点配置,通过逗号分割192.168.253.31:2181,192.168.253.32:2181localhost
#      dependencies:
#        service-zookeeper:
#          required: true
#          path: server-zookeeper
#          loadBalancerType: ROUND_ROBIN

